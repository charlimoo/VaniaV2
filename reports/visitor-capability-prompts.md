# Visitor Capability Test Prompts

Use these with `backend/definitions/agents/visitor.py`.

Replace placeholders like `<case_id>`, `<test_id>`, `<file_id>`, and `<expert_id>` with real values from earlier replies when needed.

## Basic state and context

```text
وضعیت کلی من را کوتاه مرور کن؛ پروفایل پایه‌ام، متخصص فعال، پرونده فعال، تعداد تکالیف باز، تعداد منابع و تعداد آیتم‌های مسیر را بگو.
```

```text
پروفایل کامل پایه من را از ابزارها بخوان و خیلی خلاصه جمع‌بندی کن.
```

```text
پروفایل متخصص فعال من را از ابزارها بخوان و کوتاه معرفی کن.
```

```text
لیست پرونده‌های در دسترس من را بررسی کن و اگر چند پرونده دارم، پرونده‌ها را با شناسه و عنوان کوتاه بگو.
```

```text
از روی وضعیت فعلی تشخیص بده این پرونده متعلق به چه نوع متخصصی است و بگو چه بخش‌هایی از پرونده برای من فعال است و چه بخش‌هایی ظاهراً غیرفعال یا خالی شده‌اند.
```

```text
اول مسیر من را از ابزارها بخوان، بعد خیلی کوتاه بگو الان کدام تب‌ها یا بخش‌ها برای پرونده فعال معنی‌دار هستند: overview، tasks، medications، timeline، library، files.
```

## Case switching

```text
پرونده‌های من را پیدا کن و اگر بیشتر از یکی وجود دارد، پرونده‌ای غیر از پرونده فعلی را انتخاب کن و بعد وضعیت جدید را خلاصه بگو.
```

```text
پرونده با شناسه `<case_id>` را انتخاب کن و فقط نتیجه را کوتاه گزارش بده.
```

```text
اگر چند پرونده دارم، یکی‌یکی دو پرونده مختلف را انتخاب کن و بعد تفاوت خیلی کوتاه بین وضعیت آن‌ها را بگو؛ مثلاً تعداد تکلیف، دارو، منبع یا فایل.
```

## Tasks and journey

```text
مسیر من را بارگذاری کن و بگو الان مهم‌ترین تکلیف باز من چیست. اگر چند مورد هست فقط 3 مورد اول را بگو.
```

```text
اول تکالیف باز پرونده فعال را پیدا کن، بعد یکی از آن‌ها را با یک بازخورد کوتاه به‌عنوان انجام‌شده ثبت کن، و آخرش بگو کدام تکلیف بسته شد.
```

```text
اگر تکلیف بازی دارم، یکی از آن‌ها را که مربوط‌تر است کامل‌شده ثبت کن و در بازخورد بنویس: «انجام شد و حس بهتری داشتم».
```

```text
آخرین جلسه ثبت‌شده من را پیدا کن و فلش‌کارت‌ها و خلاصه عمومی همان جلسه را کوتاه مرور کن.
```

```text
اگر جلسه ثبت‌شده‌ای دارم، فقط active goalهای فعلی من را از روی تاریخچه جلسه‌ها استخراج کن و خلاصه بگو.
```

```text
اگر هیچ تکلیف بازی نداشتم، همین را کوتاه بگو؛ اگر داشتم، یکی را کامل‌شده ثبت کن و بعد تعداد تکالیف باز باقی‌مانده را بگو.
```

## Resources and medications

```text
کتابخانه پرونده فعال را بررسی کن و اگر منبع پیشنهادیِ مصرف‌نشده وجود دارد، یکی را به‌عنوان مصرف‌شده علامت بزن و نتیجه را کوتاه بگو.
```

```text
داروهای فعلی پرونده فعال را از ابزار بخوان و خیلی کوتاه و فهرست‌وار برایم خلاصه کن.
```

```text
اگر این پرونده از نوعی است که کتابخانه یا دارو در آن نباید فعال باشد، به‌جای حدس‌زدن همان محدودیت را کوتاه توضیح بده.
```

## Tests

```text
تست‌های قابل‌مشاهده من را از پرونده فعال بررسی کن و شناسه و عنوان هرکدام را کوتاه بگو.
```

```text
جزئیات کامل نتیجه تست با شناسه `<test_id>` را بخوان و فقط خلاصه نتیجه و فایل‌های پیوست را بگو.
```

```text
نتیجه تست با شناسه `<test_id>` را با این متن به‌روزرسانی کن: «در این تست پاسخ‌ها را کامل‌تر وارد کردم و چند نشانه اصلی را توضیح دادم.» بعد تأیید کوتاه بده.
```

```text
بعد از به‌روزرسانی نتیجه تست با شناسه `<test_id>`، دوباره جزئیات همان تست را بخوان و فقط بگو متن نتیجه واقعاً تغییر کرده یا نه.
```

```text
اگر پرونده فعال من تستی ندارد یا تست‌ها برای این نوع متخصص غیرفعال‌اند، خیلی کوتاه بگو که ابزار چه چیزی برگرداند.
```

## Case sharing

```text
گزینه‌های اشتراک‌گذاری پرونده فعال را از ابزارها بخوان و فقط شناسه متخصص‌هایی را که می‌توانم با آن‌ها share کنم بگو.
```

```text
برای پرونده فعال به متخصص با شناسه `<expert_id>` دسترسی read-only بده و نتیجه را کوتاه گزارش کن.
```

```text
دسترسی read-only متخصص با شناسه `<expert_id>` را از پرونده فعال حذف کن و نتیجه را کوتاه گزارش کن.
```

```text
گزینه‌های share پرونده را بخوان و بگو الان چه shareهای فعالی وجود دارد و چه متخصص‌هایی candidate هستند.
```

```text
اگر owner پرونده یا متخصص نامناسبی در گزینه‌ها هست، تلاش کن مورد نامعتبر را share کنی و فقط خطا را کوتاه گزارش کن.
```

## Case files

```text
فایل‌های پرونده فعال را لیست کن و فقط شناسه، عنوان و نوع فایل 5 مورد اول را بگو.
```

```text
در فایل‌های پرونده فعال با عبارت «اضطراب» جست‌وجو کن و اگر نتیجه‌ای پیدا شد، فقط بهترین نتیجه‌ها را با file id و snippet کوتاه بگو.
```

```text
فایل‌های پرونده فعال را فقط از نوع pdf لیست کن و اگر چیزی پیدا شد تعدادشان را بگو.
```

```text
فقط فایل‌های خوانا را لیست کن و اگر چیزی پیدا شد 3 مورد اول را با readable بودن و page count تقریبی گزارش کن.
```

```text
جزئیات فایل با شناسه `<file_id>` را بخوان و فقط متادیتای مهم را بگو.
```

```text
فایل با شناسه `<file_id>` را در حالت excerpt بخوان و فقط همان بخش‌های مهم را خلاصه کن.
```

```text
فایل با شناسه `<file_id>` را به‌صورت page-based بخوان؛ اگر صفحه‌بندی دارد فقط صفحه 1 را خلاصه کن.
```

```text
فایل با شناسه `<file_id>` را chunk-based بخوان و فقط chunkهای 2 و 3 را خیلی کوتاه خلاصه کن.
```

```text
در داخل فایل با شناسه `<file_id>` فقط برای عبارت «بی‌خوابی» جست‌وجو کن و اگر نتیجه داشت همان را خیلی کوتاه بگو.
```

```text
اگر لازم بود اول در فایل‌های پرونده جست‌وجو کن، بعد مرتبط‌ترین فایل را باز کن و بگو آیا در آن اشاره‌ای به حمله پانیک یا بی‌خوابی شده یا نه.
```

```text
اگر فایل‌ها برای پرونده فعال غیرفعال هستند، همین محدودیت را کوتاه گزارش کن و چیزی حدس نزن.
```

## Combined flows

```text
اول وضعیت پرونده فعال را بخوان، بعد اگر تکلیف باز، منبع پیشنهادی و تست موجود بود به‌ترتیب یکی از هرکدام را مدیریت کن: task را done کن، resource را consumed کن، و test را بخوان. در پایان فقط یک جمع‌بندی سه‌خطی بده.
```

```text
ابتدا پرونده فعال را خلاصه کن، بعد اگر فایل مرتبطی پیدا شد آن را برای عبارت «اضطراب» جست‌وجو کن، و در آخر بگو آیا محتوای فایل با خلاصه بالینی پرونده هم‌راستا هست یا نه.
```

```text
اول پروفایل متخصص فعال را بخوان، بعد وضعیت داروها و جلسه آخر را مرور کن و بگو آیا بین تخصص متخصص، داروهای فعال و محتوای جلسه آخر تناسبی دیده می‌شود یا نه.
```

## Policy and restriction checks

```text
اگر پرونده فعال مربوط به روان‌شناس است، تلاش نکن دارو یا فایل را مدیریت کنی و فقط کوتاه توضیح بده چرا. اگر مربوط به روان‌پزشک است، بگو چرا task یا library ممکن است غیرفعال باشد.
```

```text
اگر پرونده فعال مربوط به وکیل یا پزشک عمومی است، بگو کدام بخش‌ها باید برای من خاموش باشند و بعد بررسی کن آیا واقعاً در snapshot فعلی خاموش شده‌اند یا نه.
```

## Edge / failure checks

```text
پرونده با شناسه `case-does-not-exist` را انتخاب کن و اگر خطا بود همان خطا را کوتاه بگو.
```

```text
جزئیات تست با شناسه `test-does-not-exist` را بخوان و اگر پیدا نشد فقط نتیجه خطا را بگو.
```

```text
فایل با شناسه `file-does-not-exist` را بخوان و اگر پیدا نشد فقط نتیجه خطا را بگو.
```

```text
یک منبع با عنوان `resource-does-not-exist` را مصرف‌شده علامت بزن و فقط نتیجه خطا را بگو.
```

```text
یک تکلیف با عنوان `task-does-not-exist` را کامل‌شده ثبت کن و فقط نتیجه خطا را بگو.
```
